<div class="container">
  <h4 class="page-heading">地域一覧</h4>
  <table class="table">
    <% @regions.each do |region| %>
      <tr>
        <td><%= link_to region.name, region_path(region.id) %></td>
      </tr>
    <% end %>
  </table>
</div>